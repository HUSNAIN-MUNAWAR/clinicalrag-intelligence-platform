'use client';
import {useState} from 'react';
import {apiPost,endpoints} from '@/lib/api';
export default function Comparison(){const [q,setQ]=useState('What warning signs are mentioned for asthma?'); const [out,setOut]=useState<any>(null); async function run(){setOut(await apiPost(endpoints.ragCompare,{question:q,pipeline_ids:['vector','bm25','hybrid','reranked_hybrid','section_aware','rag_fusion'],top_k:5,safety_mode:true}))} return <div><h1 className="text-3xl font-bold">Pipeline Comparison Lab</h1><div className="card mt-6 p-6"><input className="input w-full" value={q} onChange={e=>setQ(e.target.value)}/><button className="btn mt-4" onClick={run}>Compare pipelines</button></div>{out&&<div className="mt-6 grid grid-cols-2 gap-4">{out.comparisons.map((r:any)=><div className="card p-5" key={r.pipeline_id}><h2 className="font-semibold">{r.pipeline_id}</h2><div className="text-sm text-slate-500">citations {r.citation_count} • risk {r.hallucination_risk}</div><pre className="mt-3 overflow-auto rounded-xl bg-slate-950 p-3 text-xs text-slate-100">{JSON.stringify(r.top_documents,null,2)}</pre></div>)}</div>}</div>}
