export default function Settings(){return <div><h1 className="text-3xl font-bold">Settings</h1><div className="card mt-6 p-6 space-y-3 text-sm"><div>LLM provider: retrieval-only by default; set backend env vars for Ollama or OpenAI-compatible providers.</div><div>Embedding provider: deterministic local hash embeddings for demo; SentenceTransformers/OpenAI-compatible extension points are in backend/app/rag/embeddings.py.</div><div>Vector database: local JSON default; Qdrant/Chroma/pgvector can be swapped behind VectorStore.</div><div>Safety strictness: conservative rule-based medical safety classifier enabled by default.</div></div></div>}
