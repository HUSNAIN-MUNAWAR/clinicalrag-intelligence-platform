'use client';
import {useEffect,useState} from 'react';
import {apiGet,apiPost,endpoints} from '@/lib/api';
import type {Pipeline,RagAnswer} from '@/types/api';
export default function Playground(){const [pipelines,setPipelines]=useState<Pipeline[]>([]); const [q,setQ]=useState('What are common symptoms of hypertension?'); const [pid,setPid]=useState('hybrid'); const [ans,setAns]=useState<RagAnswer|null>(null); useEffect(()=>{apiGet<Pipeline[]>(endpoints.pipelines).then(setPipelines)},[]); async function ask(){setAns(await apiPost<RagAnswer>(endpoints.ragAsk,{question:q,pipeline_id:pid,top_k:6,safety_mode:true}))} return <div><h1 className="text-3xl font-bold">RAG Playground</h1><div className="card mt-6 p-6 space-y-4"><textarea className="input h-28 w-full" value={q} onChange={e=>setQ(e.target.value)} /><select className="input" value={pid} onChange={e=>setPid(e.target.value)}>{pipelines.map(p=><option key={p.id} value={p.id}>{p.name}{p.experimental?' (experimental)':''}</option>)}</select><button className="btn" onClick={ask}>Ask with citations</button></div>{ans&&<div className="card mt-6 p-6"><div className="text-sm text-slate-500">{ans.pipeline_used} • {ans.confidence_level} • {ans.latency_breakdown.total_ms} ms</div><p className="mt-4 whitespace-pre-wrap">{ans.answer}</p><h3 className="mt-6 font-semibold">Citations</h3><div className="mt-2 space-y-2">{ans.citations.map(c=><div key={c.citation_id} className="rounded-xl border p-3 text-sm"><b>{c.citation_id}</b> {c.title} — {c.section}<div className="text-slate-500">{c.quote}</div></div>)}</div><h3 className="mt-6 font-semibold">Retrieved evidence</h3><pre className="mt-2 max-h-96 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-100">{JSON.stringify(ans.retrieved_chunks,null,2)}</pre></div>}</div>}
